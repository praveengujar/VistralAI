# VistralAI Environment Variables
# Copy this file to .env.local and fill in the values

# ============================================================================
# Authentication & Security
# ============================================================================

# NextAuth.js configuration
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Your app URL (for local development, use http://localhost:3000)
# For production, use your actual domain
NEXTAUTH_URL=http://localhost:3000

# ============================================================================
# AI & LLM Integration
# ============================================================================

# Anthropic Claude API
# Get from: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Optional: OpenAI Integration (future use)
# OPENAI_API_KEY=sk-your-openai-key

# ============================================================================
# Database Configuration (when migrating from mock data)
# ============================================================================

# PostgreSQL (recommended)
# DATABASE_URL=postgresql://user:password@localhost:5432/vistralai

# MongoDB (alternative)
# MONGODB_URI=mongodb://localhost:27017/vistralai
# MONGODB_DB=vistralai

# ============================================================================
# Queue & Jobs (when moving beyond in-memory queue)
# ============================================================================

# Redis for job queue (Bull)
# REDIS_URL=redis://localhost:6379

# Optional: Upstash Redis (serverless alternative)
# UPSTASH_REDIS_URL=redis://default:token@...

# ============================================================================
# Integrations (Shopify, etc.)
# ============================================================================

# Shopify OAuth (future feature)
# SHOPIFY_CLIENT_ID=your-client-id
# SHOPIFY_CLIENT_SECRET=your-client-secret
# SHOPIFY_SCOPES=read_products,read_product_listings

# ============================================================================
# Web Crawler Configuration
# ============================================================================

# Maximum pages to crawl per website
CRAWLER_MAX_PAGES=10

# Crawler timeout (milliseconds)
CRAWLER_TIMEOUT_MS=30000

# Crawler User-Agent
CRAWLER_USER_AGENT=VistralAI-Bot/1.0

# Enable/disable robots.txt respect
CRAWLER_RESPECT_ROBOTS_TXT=true

# ============================================================================
# Monitoring & Analytics (optional)
# ============================================================================

# Sentry for error tracking (future)
# SENTRY_DSN=https://...@sentry.io/...

# PostHog for analytics (future)
# POSTHOG_API_KEY=your-api-key
# POSTHOG_ENDPOINT=https://app.posthog.com

# ============================================================================
# Email Configuration (future)
# ============================================================================

# SendGrid for transactional emails
# SENDGRID_API_KEY=SG.your-api-key

# Or use other providers
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ============================================================================
# Feature Flags
# ============================================================================

# Enable/disable new AI-powered onboarding flow
ENABLE_AI_ONBOARDING=true

# Enable/disable Shopify integration
ENABLE_SHOPIFY=false

# Enable/disable real API calls (vs mock data)
USE_REAL_LLM_API=false

# ============================================================================
# Development & Debugging
# ============================================================================

# Enable debug logging
DEBUG=false

# Log level: 'debug' | 'info' | 'warn' | 'error'
LOG_LEVEL=info

# ============================================================================
# Node Environment
# ============================================================================

NODE_ENV=development

# ============================================================================
# Notes:
# ============================================================================

# 1. For local development with mock data:
#    - NEXTAUTH_SECRET: Any random string
#    - NEXTAUTH_URL: http://localhost:3000
#    - ANTHROPIC_API_KEY: Leave empty (will use mock data)
#    - USE_REAL_LLM_API: false
#
# 2. For production with real APIs:
#    - Generate proper NEXTAUTH_SECRET
#    - Set NEXTAUTH_URL to your domain
#    - Add ANTHROPIC_API_KEY
#    - Set USE_REAL_LLM_API=true
#    - Configure database (PostgreSQL recommended)
#    - Configure Redis for job queue
#    - Set up error tracking (Sentry)
#
# 3. For Google Cloud Run deployment:
#    - Use Google Secret Manager instead of .env
#    - Set env vars via Cloud Run console or gcloud CLI:
#      gcloud run services update vistralai \
#        --update-env-vars KEY=value \
#        --set-secrets KEY=secret:version \
#        --region us-central1
